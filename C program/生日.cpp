#include<cstdio>
#include<iostream>
#include<algorithm>
using namespace std;
struct bir
{
	char s[20];
	int y;
	int m;
	int d;
}a[100];

bool cmp(bir s1,bir s2)
{
	if(s1.y==s2.y)
	{
		if(s1.m==s2.m)
		{
			if(s1.d==s2.d)
		}
	}
}